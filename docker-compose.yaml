version: '3.8'

services:
  next-js:
    image: vavra/project-united_next-js
    container_name: project-united_next-js
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        NODE_VERSION: 14.4
    ports:
      - '3000:3000'
    volumes:
      - type: bind
        source: ./
        target: /usr/src/app
